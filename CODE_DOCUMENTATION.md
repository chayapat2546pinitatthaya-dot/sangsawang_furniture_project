# 📚 เอกสารอธิบายโครงสร้างโค้ด - Sangsawang Furniture

เอกสารนี้อธิบายโครงสร้างโค้ดทั้งหมดของโปรเจกต์ เพื่อใช้ในการพรีเซนต์และอธิบายให้อาจารย์เข้าใจ

---

## 📑 สารบัญ

1. [โครงสร้างโปรเจกต์](#โครงสร้างโปรเจกต์)
2. [Backend (server.js)](#backend-serverjs)
3. [Frontend (React)](#frontend-react)
4. [Database Schema](#database-schema)
5. [ส่วนสำคัญสำหรับการพรีเซนต์](#ส่วนสำคัญสำหรับการพรีเซนต์)
6. [API Endpoints แบบละเอียด](#api-endpoints-แบบละเอียด)
7. [Security Features](#security-features)
8. [Flow การทำงานของระบบ](#flow-การทำงานของระบบ)

---

## 📁 โครงสร้างโปรเจกต์

```
Final-Project-main/
├── server.js                    # Backend Server (ไฟล์หลัก - 2262 บรรทัด)
├── package.json                  # Backend Dependencies
├── .env                         # Environment Variables (ไม่ commit)
│
├── database/
│   ├── schema.sql               # Database Schema + Sample Data
│   └── import_from_phpmyadmin.sql  # SQL Dump จาก phpMyAdmin
│
├── images/                      # รูปภาพสินค้า (static files)
│   ├── โซฟา/
│   ├── ตู้เสื้อผ้า/
│   ├── เตียงนอน/
│   └── ...
│
└── client/                      # Frontend (React)
    ├── package.json
    ├── public/
    │   └── index.html
    └── src/
        ├── App.js               # Main App Component (Routing)
        ├── index.js             # React Entry Point
        ├── components/          # Reusable Components
        │   ├── Navbar.js        # Navigation Bar
        │   └── ...
        └── pages/               # Page Components
            ├── Home.js           # หน้าแรก
            ├── Products.js       # หน้าสินค้าทั้งหมด
            ├── ProductDetail.js  # รายละเอียดสินค้า
            ├── Cart.js           # ตะกร้าสินค้า
            ├── Login.js          # เข้าสู่ระบบ
            ├── Register.js       # สมัครสมาชิก
            ├── Profile.js        # ข้อมูลส่วนตัว
            ├── Orders.js         # คำสั่งซื้อ
            └── admin/            # หน้า Admin
                ├── AdminLogin.js
                ├── AdminDashboard.js
                ├── AdminProducts.js
                └── AdminOrders.js
```

---

## 🔧 Backend (server.js)

### โครงสร้างไฟล์ server.js

ไฟล์ `server.js` มี 2262 บรรทัด แบ่งเป็นส่วนต่างๆ ดังนี้:

#### 1. **ส่วน Import และ Setup (บรรทัด 1-16)**
```javascript
// โหลด environment variables จากไฟล์ .env
require('dotenv').config({ override: true });

// Import libraries ที่จำเป็น
const express = require('express');      // Web framework
const mysql = require('mysql2/promise');  // MySQL driver
const cors = require('cors');            // Cross-Origin Resource Sharing
const bcrypt = require('bcryptjs');      // Password hashing
const jwt = require('jsonwebtoken');     // JWT authentication
const nodemailer = require('nodemailer'); // Email sending
const crypto = require('crypto');        // Cryptography utilities
const multer = require('multer');        // File upload handling
const fs = require('fs');                // File system operations
```

**ความสำคัญ:**
- เป็นส่วนที่กำหนด dependencies ทั้งหมดของโปรเจกต์
- แต่ละ library มีบทบาทสำคัญในการทำงานของระบบ:
  - **express**: Web framework หลักที่ใช้สร้าง RESTful API
  - **mysql2/promise**: Database driver ที่รองรับ async/await ทำให้เขียนโค้ดได้สะอาดขึ้น
  - **cors**: อนุญาตให้ Frontend (React) ที่รันบน port ต่างกันเรียก API ได้
  - **bcryptjs**: Hash password เพื่อความปลอดภัย ไม่เก็บรหัสผ่านแบบ plain text
  - **jsonwebtoken**: สร้างและตรวจสอบ JWT token สำหรับ authentication
  - **nodemailer**: ส่งอีเมลแจ้งเตือนลูกค้าเมื่อมีการอนุมัติคำสั่งซื้อ
  - **multer**: จัดการการอัปโหลดไฟล์รูปภาพสินค้า
  - **fs**: อ่าน/เขียนไฟล์ในระบบ เช่น การลบรูปภาพเก่าเมื่ออัปเดตสินค้า
- ถ้าไม่มีส่วนนี้ ระบบจะไม่สามารถทำงานได้เลย

#### 2. **Middleware Configuration (บรรทัด 17-98)**
```javascript
// CORS - อนุญาตให้ Frontend (port 3001) เรียก API จาก Backend (port 7100)
app.use(cors());

// JSON Parser - แปลง JSON request body เป็น JavaScript object
app.use(express.json());

// URL Encoded Parser - แปลง form data เป็น JavaScript object
app.use(express.urlencoded({ extended: true }));

// Multer Configuration - ตั้งค่าการอัปโหลดไฟล์
// - เก็บไฟล์ในโฟลเดอร์ images/
// - จำกัดขนาดไฟล์ 10MB
// - อนุญาตเฉพาะไฟล์ภาพ (jpg, png, gif, webp)

// Static Files - ให้บริการไฟล์รูปภาพจากโฟลเดอร์ images/
app.use('/images', express.static(path.join(__dirname, 'images')));
```

**ความสำคัญ:**
- **Middleware คืออะไร**: Function ที่ทำงานระหว่างการรับ request และส่ง response กลับไป เปรียบเสมือน "ด่านตรวจ" ที่ข้อมูลต้องผ่านก่อนถึง endpoint จริง
- **แต่ละ middleware ทำหน้าที่อะไร**:
  - **CORS**: อนุญาตให้ Frontend (port 3001) เรียก API จาก Backend (port 7100) ได้ โดยไม่มี browser block เนื่องจากเป็น cross-origin request
  - **express.json()**: แปลง JSON request body (เช่น `{"username": "test"}`) เป็น JavaScript object ที่ใช้งานได้ (`req.body.username`)
  - **express.urlencoded()**: แปลง form data (เช่น `username=test&password=123`) เป็น JavaScript object
  - **Multer**: ตรวจสอบและจัดการไฟล์ที่อัปโหลด (จำกัดขนาด 10MB, อนุญาตเฉพาะไฟล์ภาพ) เพื่อป้องกันการอัปโหลดไฟล์อันตราย
  - **Static Files**: ให้บริการไฟล์รูปภาพจากโฟลเดอร์ `images/` ผ่าน URL เช่น `http://localhost:7100/images/โซฟา/image.jpg`
- **ทำไมต้องมี**: ถ้าไม่มี middleware เหล่านี้ ระบบจะไม่สามารถรับข้อมูลจาก Frontend, อัปโหลดไฟล์, หรือให้บริการรูปภาพได้

#### 3. **Database Configuration (บรรทัด 100-197)**
```javascript
// Database Configuration
const dbConfig = {
    host: process.env.DB_HOST || 'localhost',
    port: Number(process.env.DB_PORT) || 3306,  // MAMP: 8889, XAMPP: 3306
    user: process.env.DB_USER || 'root',
    password: process.env.DB_PASSWORD || '',
    database: process.env.DB_NAME || 'sangsawang_furniture'
};

// Connection Pool - สร้าง pool ของ database connections
const pool = mysql.createPool(dbConfig);

// Database Migration Functions
// - ensureCustomerAltAddressesColumn() - เพิ่ม column สำหรับที่อยู่สำรอง
// - ensureCustomerEmailVerificationColumns() - เพิ่ม columns สำหรับยืนยันอีเมล
// - ensureOrderShippingAddressColumn() - เพิ่ม column สำหรับที่อยู่จัดส่ง
// - ensureCartItemsTable() - สร้างตาราง cart_item ถ้ายังไม่มี
```

**ความสำคัญ:**
- **Connection Pool คืออะไร**: 
  - เป็นการสร้าง "สระ" ของ database connections ล่วงหน้า (เช่น 10 connections)
  - เมื่อต้องการใช้ database จะ "ยืม" connection จาก pool
  - ใช้เสร็จแล้ว "คืน" connection กลับไป pool แทนการปิดทิ้ง
  - เปรียบเทียบได้กับห้องสมุดที่ให้ยืมหนังสือ แทนการซื้อหนังสือใหม่ทุกครั้งที่ต้องการอ่าน

- **ทำไมต้องใช้ Pool แทน Connection ธรรมดา**:
  - **ประสิทธิภาพดีกว่า**: การสร้าง connection ใหม่ทุกครั้งใช้เวลาและทรัพยากรมาก (ประมาณ 50-100ms) แต่การใช้ pool ใช้เวลาเพียง 1-2ms
  - **รองรับ concurrent requests**: เมื่อมีผู้ใช้หลายคนเรียก API พร้อมกัน Pool จะแจกจ่าย connections ให้อัตโนมัติ
  - **ป้องกัน connection exhaustion**: จำกัดจำนวน connections สูงสุด (เช่น 10 connections) เพื่อไม่ให้ database รับไม่ไหว
  - **ตัวอย่าง**: ถ้ามี 100 requests พร้อมกัน แต่ใช้ connection ธรรมดา จะต้องสร้าง 100 connections ซึ่ง database อาจรับไม่ไหว แต่ถ้าใช้ pool จะมีเพียง 10 connections ที่ reuse กัน

- **Migration functions ทำหน้าที่อะไร**:
  - **ensureCustomerAltAddressesColumn()**: เพิ่ม column `customer_alt_addresses` ถ้ายังไม่มี (รองรับที่อยู่สำรองหลายที่)
  - **ensureCustomerEmailVerificationColumns()**: เพิ่ม columns สำหรับระบบยืนยันอีเมล (OTP, verification status)
  - **ensureOrderShippingAddressColumn()**: เพิ่ม column สำหรับเก็บที่อยู่จัดส่งใน order
  - **ensureCartItemsTable()**: สร้างตาราง `cart_item` ถ้ายังไม่มี (รองรับการเก็บตะกร้าใน database)
  - **ทำไมต้องมี**: ช่วยให้ระบบสามารถอัปเดตโครงสร้าง database อัตโนมัติเมื่อเริ่มต้น server โดยไม่ต้องแก้ไข database ด้วยมือ

#### 4. **Helper Functions (บรรทัด 199-737)**

##### 4.1 Email Functions
```javascript
// sendSystemEmail() - ส่งอีเมลผ่าน SMTP
// buildAwaitingPaymentEmail() - สร้างเนื้อหาอีเมลแจ้งการอนุมัติคำสั่งซื้อ
// buildEmailVerificationOtpEmail() - สร้างเนื้อหาอีเมล OTP
// generateEmailVerificationOtp() - สร้างรหัส OTP 6 หลัก
```

##### 4.2 Image Processing Functions
```javascript
// parseProductImages() - แปลง product_image จาก JSON string เป็น array
// getPrimaryProductImage() - ดึงรูปภาพหลักของสินค้า
// normalizeImagePath() - ปรับ path รูปภาพให้มี /images/ prefix
// normalizeImagesInput() - ปรับ input รูปภาพให้เป็น array ที่ถูกต้อง
```

##### 4.3 Data Normalization Functions
```javascript
// toCleanString() - ทำความสะอาด string (trim, handle null/undefined)
// sanitizePhone() - ทำความสะอาดเบอร์โทรศัพท์ (ลบตัวอักษรที่ไม่ใช่ตัวเลข)
// normalizeShippingRecord() - ปรับข้อมูลที่อยู่จัดส่งให้เป็นรูปแบบมาตรฐาน
// normalizeShippingCollection() - ปรับ collection ของที่อยู่ให้เป็น array
```

##### 4.4 Cart Functions
```javascript
// resolveCartBasePrice() - คำนวณราคาพื้นฐานของสินค้าในตะกร้า
// normalizeCartItemRow() - ปรับข้อมูล cart item ให้เป็นรูปแบบมาตรฐาน
// getCartItemsForCustomer() - ดึงข้อมูลสินค้าในตะกร้าของลูกค้า
// calculateCartCount() - นับจำนวนสินค้าในตะกร้า
```

##### 4.5 Order Functions
```javascript
// fetchOrderWithCustomer() - ดึงข้อมูลคำสั่งซื้อพร้อมข้อมูลลูกค้า
// setOrderStatus() - เปลี่ยนสถานะคำสั่งซื้อ
// normalizeOrderStatus() - ปรับสถานะคำสั่งซื้อให้เป็นรูปแบบมาตรฐาน
// getOrderStatusLabel() - แปลงสถานะเป็นข้อความภาษาไทย
```

**ความสำคัญ:**
- **ช่วยให้โค้ดอ่านง่ายและ reuse ได้**:
  - **DRY Principle (Don't Repeat Yourself)**: แทนการเขียนโค้ดซ้ำๆ หลายที่ ให้เขียนเป็น function หนึ่งครั้งแล้วเรียกใช้ได้ทุกที่
  - **ตัวอย่าง**: ถ้าต้องการ parse รูปภาพสินค้าใน 5 endpoints แทนการเขียนโค้ด 5 ชุด ให้เขียน `parseProductImages()` หนึ่งครั้งแล้วเรียกใช้
  - **ง่ายต่อการแก้ไข**: ถ้าต้องการเปลี่ยนวิธีการ parse รูปภาพ แก้ที่เดียวก็ใช้ได้ทุกที่

- **แยกหน้าที่ชัดเจน (Separation of Concerns)**:
  - **Email Functions**: จัดการเรื่องอีเมลทั้งหมด (ส่งอีเมล, สร้างเนื้อหา, สร้าง OTP)
  - **Image Processing Functions**: จัดการเรื่องรูปภาพทั้งหมด (parse, normalize path, ดึงรูปหลัก)
  - **Data Normalization Functions**: ทำความสะอาดข้อมูลก่อนเก็บใน database (trim whitespace, sanitize phone)
  - **Cart Functions**: จัดการตะกร้าสินค้า (คำนวณราคา, ดึงข้อมูล, นับจำนวน)
  - **Order Functions**: จัดการคำสั่งซื้อ (ดึงข้อมูล, เปลี่ยนสถานะ, แปลงเป็นภาษาไทย)

- **ประโยชน์อื่นๆ**:
  - **ง่ายต่อการทดสอบ**: สามารถทดสอบแต่ละ function แยกกันได้
  - **ง่ายต่อการ debug**: เมื่อเกิด error รู้ทันทีว่าเกิดจาก function ไหน
  - **ง่ายต่อการทำงานเป็นทีม**: แต่ละคนสามารถทำงานกับ function ที่แตกต่างกันได้โดยไม่ชนกัน
  - **ลดความซับซ้อน**: Endpoint หลักจะสั้นและอ่านง่าย เพราะ logic ซับซ้อนถูกย้ายไปอยู่ใน helper functions

#### 5. **Authentication Middleware (บรรทัด 739-754)**
```javascript
// authenticateToken() - ตรวจสอบ JWT token
// 1. อ่าน token จาก Authorization header
// 2. Verify token ด้วย JWT_SECRET
// 3. ถ้าถูกต้อง เก็บ user info ใน req.user
// 4. ถ้าไม่ถูกต้อง ส่ง error 401/403
```

**ความสำคัญ:**
- **JWT คืออะไร**:
  - **JSON Web Token** เป็นมาตรฐานสำหรับการส่งข้อมูลที่ถูกเข้ารหัสระหว่าง parties
  - ประกอบด้วย 3 ส่วน: Header (algorithm), Payload (ข้อมูล user), Signature (ลายเซ็นเพื่อป้องกันการแก้ไข)
  - **ตัวอย่าง**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwidXNlcm5hbWUiOiJ0ZXN0In0.signature`
  - เก็บข้อมูล user (id, username, role) ใน token เอง ไม่ต้อง query database ทุกครั้ง

- **ทำไมต้องใช้ middleware**:
  - **ป้องกันการเข้าถึงข้อมูลโดยไม่ได้รับอนุญาต**: ตรวจสอบ token ก่อนเข้าถึง protected routes (เช่น `/api/customer/profile`, `/api/orders`)
  - **ไม่ต้องเขียนโค้ดซ้ำ**: แทนการตรวจสอบ token ในทุก endpoint ให้เขียน middleware หนึ่งครั้งแล้วใช้กับทุก route
  - **Centralized Logic**: ถ้าต้องการเปลี่ยนวิธีการ authentication (เช่นเพิ่ม refresh token) แก้ที่เดียวก็ใช้ได้ทุกที่
  - **ตัวอย่าง**: ถ้าไม่มี middleware จะต้องเขียนโค้ดตรวจสอบ token ใน 20+ endpoints ซึ่งซ้ำซ้อนและเสี่ยงต่อการลืมตรวจสอบ

- **วิธีการทำงานของ authentication**:
  1. **User Login**: Server ตรวจสอบ username/password → สร้าง JWT token → ส่งกลับไป Frontend
  2. **Frontend เก็บ token**: เก็บใน `localStorage` เพื่อใช้ใน request ถัดไป
  3. **ส่ง token ใน request**: Frontend ส่ง token ใน header `Authorization: Bearer <token>`
  4. **Middleware ตรวจสอบ**: 
     - อ่าน token จาก header
     - Verify signature ด้วย `JWT_SECRET` (ถ้าแก้ไข token จะ verify ไม่ผ่าน)
     - ตรวจสอบ expiration (token หมดอายุ 24 ชั่วโมง)
     - ถ้าถูกต้อง เก็บ user info ใน `req.user` และเรียก `next()` เพื่อไปยัง endpoint
     - ถ้าไม่ถูกต้อง ส่ง error 401/403 กลับไป
  5. **Endpoint ใช้งาน**: ใช้ `req.user.id` เพื่อดึงข้อมูลของ user นั้นๆ

- **ข้อดีของ JWT**:
  - **Stateless**: Server ไม่ต้องเก็บ session ทำให้ scalable มากขึ้น
  - **Cross-domain**: ใช้ได้กับหลาย domain (Frontend และ Backend แยกกัน)
  - **Self-contained**: ข้อมูล user อยู่ใน token เอง ไม่ต้อง query database

#### 6. **API Routes (บรรทัด 756-2254)**

##### 6.1 Public Routes
```javascript
GET /api/health              // Health check
GET /api/products            // ดึงสินค้าทั้งหมด
GET /api/products/:id        // ดึงสินค้า 1 รายการ
GET /api/categories          // ดึงหมวดหมู่ทั้งหมด
```

##### 6.2 Customer Routes
```javascript
POST /api/customer/register          // สมัครสมาชิก
POST /api/customer/login             // เข้าสู่ระบบ
GET  /api/customer/profile           // ดูข้อมูลส่วนตัว
PUT  /api/customer/profile           // แก้ไขข้อมูลส่วนตัว
PUT  /api/customer/password          // เปลี่ยนรหัสผ่าน
POST /api/customer/verify-email      // ยืนยันอีเมลด้วย OTP
POST /api/customer/resend-verification  // ส่ง OTP ใหม่
GET  /api/customer/orders            // ดูคำสั่งซื้อของตัวเอง
```

##### 6.3 Cart Routes
```javascript
GET    /api/cart              // ดึงสินค้าในตะกร้า
PUT    /api/cart/items        // เพิ่ม/แก้ไขสินค้าในตะกร้า
DELETE /api/cart/items        // ลบสินค้าออกจากตะกร้า
DELETE /api/cart              // ล้างตะกร้าทั้งหมด
```

##### 6.4 Order Routes
```javascript
POST /api/orders              // สร้างคำสั่งซื้อ
GET  /api/orders/:id          // ดูรายละเอียดคำสั่งซื้อ
PUT  /api/orders/:id/status   // เปลี่ยนสถานะคำสั่งซื้อ (Admin)
PUT  /api/orders/approve/:id // อนุมัติคำสั่งซื้อ (Admin)
PUT  /api/orders/reject/:id   // ปฏิเสธคำสั่งซื้อ (Admin)
PUT  /api/orders/cancel/:id   // ยกเลิกคำสั่งซื้อ (Customer)
```

##### 6.5 Product Routes (Admin)
```javascript
POST   /api/products          // เพิ่มสินค้า
PUT    /api/products/:id      // แก้ไขสินค้า
DELETE /api/products/:id      // ลบสินค้า
POST   /api/upload             // อัปโหลดรูปภาพ
```

##### 6.6 Admin Routes
```javascript
POST /api/admin/login         // เข้าสู่ระบบแอดมิน
GET  /api/admin/customers     // ดูลูกค้าทั้งหมด
GET  /api/admin/customers/:id // ดูรายละเอียดลูกค้า
```

**ความสำคัญ:**
- **RESTful API design คืออะไร**:
  - **REST (Representational State Transfer)** เป็น architectural style สำหรับการออกแบบ API
  - ใช้ HTTP methods ตามความหมาย: GET (ดึงข้อมูล), POST (สร้างใหม่), PUT (อัปเดต), DELETE (ลบ)
  - **URL structure**: `/api/resource/:id` (เช่น `/api/products/1` = สินค้ารายการที่ 1)
  - **Stateless**: แต่ละ request เป็นอิสระจากกัน ไม่ต้องเก็บ state
  - **Uniform Interface**: ใช้รูปแบบเดียวกันทั้งระบบ ทำให้เรียนรู้และใช้งานง่าย

- **แต่ละ endpoint ทำหน้าที่อะไร**:
  - **Public Routes**: เปิดให้ทุกคนเข้าถึงได้ (ไม่ต้อง login) เช่น ดูสินค้า, ดูหมวดหมู่
  - **Customer Routes**: ต้อง login เป็น customer ก่อน เช่น ดูข้อมูลส่วนตัว, ดูคำสั่งซื้อ
  - **Cart Routes**: จัดการตะกร้าสินค้า (เพิ่ม, แก้ไข, ลบ, ล้าง)
  - **Order Routes**: สร้างคำสั่งซื้อ, ดูรายละเอียด, เปลี่ยนสถานะ (Admin), ยกเลิก (Customer)
  - **Product Routes (Admin)**: เพิ่ม/แก้ไข/ลบสินค้า (เฉพาะ Admin)
  - **Admin Routes**: จัดการลูกค้า, ดูสถิติ (เฉพาะ Admin)

- **วิธีการทำงานของแต่ละ endpoint**:
  1. **รับ Request**: รับข้อมูลจาก Frontend (JSON body, query parameters, URL parameters)
  2. **ตรวจสอบ Authentication**: ใช้ middleware `authenticateToken()` เพื่อตรวจสอบ token (สำหรับ protected routes)
  3. **ตรวจสอบ Authorization**: ใช้ middleware `isAdmin()` เพื่อตรวจสอบสิทธิ์ (สำหรับ Admin routes)
  4. **Validate Input**: ตรวจสอบว่าข้อมูลที่ส่งมาถูกต้อง (เช่น email format, required fields)
  5. **Business Logic**: ประมวลผลข้อมูล (query database, คำนวณ, สร้าง token, ส่งอีเมล)
  6. **Error Handling**: ถ้าเกิด error ให้ catch และส่ง error message กลับไป
  7. **ส่ง Response**: ส่งข้อมูลกลับไป Frontend ในรูปแบบ JSON

- **ตัวอย่าง Flow**:
  ```
  Frontend: POST /api/customer/login {username: "test", password: "123"}
    ↓
  Backend: ตรวจสอบ username/password ใน database
    ↓
  Backend: สร้าง JWT token
    ↓
  Backend: ส่ง response {token: "...", user: {...}}
    ↓
  Frontend: เก็บ token ใน localStorage และ redirect ไปหน้า Home
  ```

- **ประโยชน์ของ RESTful API**:
  - **Standardized**: ใช้มาตรฐานเดียวกันทั้งระบบ ทำให้ Frontend และ Backend เข้าใจกันง่าย
  - **Scalable**: แยก Frontend และ Backend ได้ชัดเจน สามารถ scale แต่ละส่วนได้อิสระ
  - **Maintainable**: โครงสร้างชัดเจน ง่ายต่อการเพิ่ม feature ใหม่หรือแก้ไข
  - **Testable**: สามารถทดสอบแต่ละ endpoint แยกกันได้

---

## ⚛️ Frontend (React)

### โครงสร้าง Frontend

#### 1. **App.js - Main Router**
```javascript
// หน้าที่:
// - กำหนด routes ทั้งหมด
// - จัดการ authentication state
// - Protected routes (ต้อง login ก่อนเข้า)
// - Role-based routing (Customer vs Admin)
```

**Routes:**
- `/` - หน้าแรก (Home)
- `/products` - สินค้าทั้งหมด
- `/products/:id` - รายละเอียดสินค้า
- `/cart` - ตะกร้าสินค้า
- `/login` - เข้าสู่ระบบ
- `/register` - สมัครสมาชิก
- `/profile` - ข้อมูลส่วนตัว
- `/orders` - คำสั่งซื้อ
- `/admin/login` - เข้าสู่ระบบแอดมิน
- `/admin/dashboard` - แดชบอร์ดแอดมิน
- `/admin/products` - จัดการสินค้า
- `/admin/orders` - จัดการคำสั่งซื้อ

#### 2. **Components**

##### Navbar.js
- Navigation bar ที่แสดงทุกหน้า
- แสดงข้อมูลผู้ใช้ที่ login
- มีเมนูแยกสำหรับ Customer และ Admin

##### Cards.js
- Component สำหรับแสดงสินค้าเป็น card
- ใช้ในหน้า Home และ Products

#### 3. **Pages**

##### Customer Pages
1. **Home.js** - หน้าแรก
   - แสดงสินค้าแนะนำ
   - Hero section
   - Features section

2. **Products.js** - สินค้าทั้งหมด
   - แสดงสินค้าทั้งหมด
   - ระบบค้นหา
   - Filter by category

3. **ProductDetail.js** - รายละเอียดสินค้า
   - แสดงรูปภาพสินค้า
   - ข้อมูลสินค้า
   - ราคา (เงินสด/ผ่อน)
   - ปุ่มเพิ่มลงตะกร้า

4. **Cart.js** - ตะกร้าสินค้า
   - แสดงสินค้าในตะกร้า
   - แก้ไขจำนวน
   - เลือกวิธีการชำระเงิน
   - เลือกจำนวนงวดผ่อน
   - สั่งซื้อ

5. **Login.js** - เข้าสู่ระบบ
   - Form สำหรับ login
   - Validation
   - Error handling

6. **Register.js** - สมัครสมาชิก
   - Form สำหรับสมัครสมาชิก
   - Validation
   - Password confirmation

7. **Profile.js** - ข้อมูลส่วนตัว
   - แสดงข้อมูลปัจจุบัน
   - แก้ไขข้อมูล
   - เปลี่ยนรหัสผ่าน

8. **Orders.js** - คำสั่งซื้อ
   - แสดงรายการคำสั่งซื้อ
   - ดูรายละเอียดแต่ละคำสั่ง
   - ตารางผ่อนชำระ

##### Admin Pages
1. **AdminLogin.js** - เข้าสู่ระบบแอดมิน
2. **AdminDashboard.js** - แดชบอร์ด
   - สถิติรวม
   - Quick links
3. **AdminProducts.js** - จัดการสินค้า
   - CRUD operations
   - Upload images
4. **AdminOrders.js** - จัดการคำสั่งซื้อ
   - ดูคำสั่งซื้อทั้งหมด
   - อนุมัติ/ปฏิเสธ
5. **AdminCustomers.js** - จัดการลูกค้า
   - ดูลูกค้าทั้งหมด
   - ดูรายละเอียดลูกค้า

---

## 🗄️ Database Schema

### Tables

1. **admin** - ข้อมูลแอดมิน
2. **customer** - ข้อมูลลูกค้า
3. **category** - หมวดหมู่สินค้า
4. **product** - ข้อมูลสินค้า
5. **cart_item** - สินค้าในตะกร้า
6. **order** - คำสั่งซื้อ
7. **order_detail** - รายละเอียดสินค้าในคำสั่งซื้อ
8. **installment_payments** - รายละเอียดการผ่อนชำระ

### Relationships

```
customer (1) ──→ (N) order
order (1) ──→ (N) order_detail
order (1) ──→ (N) installment_payments
product (1) ──→ (N) order_detail
product (1) ──→ (N) cart_item
customer (1) ──→ (N) cart_item
category (1) ──→ (N) product
```

---

## 🎯 ส่วนสำคัญสำหรับการพรีเซนต์

### 1. **Authentication System (JWT)**

**ตำแหน่ง:** `server.js` บรรทัด 739-754, 815-861, 1295-1357

**สิ่งที่ควรอธิบาย:**
- JWT คืออะไร
- ทำไมต้องใช้ JWT แทน session
- วิธีการทำงาน:
  1. User login → Server สร้าง JWT token
  2. Client เก็บ token ใน localStorage
  3. Client ส่ง token ใน Authorization header ทุก request
  4. Server verify token ก่อนเข้าถึง protected routes

**Code ที่สำคัญ:**
```javascript
// สร้าง token เมื่อ login
const token = jwt.sign(
    { id: user.customer_id, username: user.customer_username, role: 'customer' },
    process.env.JWT_SECRET,
    { expiresIn: '24h' }
);

// ตรวจสอบ token
jwt.verify(token, process.env.JWT_SECRET, (err, user) => {
    if (err) return res.status(403).json({ error: 'Invalid token.' });
    req.user = user;
    next();
});
```

### 2. **Password Hashing (bcrypt)**

**ตำแหน่ง:** `server.js` บรรทัด 782, 829-852, 1129

**สิ่งที่ควรอธิบาย:**
- ทำไมต้อง hash password
- bcrypt คืออะไร
- วิธีการทำงาน:
  1. เมื่อสมัครสมาชิก → hash password ก่อนเก็บใน database
  2. เมื่อ login → เปรียบเทียบ password ที่ hash แล้ว
  3. ไม่สามารถ reverse hash กลับเป็น password เดิมได้

**Code ที่สำคัญ:**
```javascript
// Hash password
const hashedPassword = await bcrypt.hash(password, 10);

// เปรียบเทียบ password
const validPassword = await bcrypt.compare(password, user.customer_password);
```

### 3. **ระบบผ่อนชำระ**

**ตำแหน่ง:** `server.js` บรรทัด 2074-2194

**สิ่งที่ควรอธิบาย:**
- วิธีการคำนวณยอดผ่อน
- การสร้างตารางผ่อนชำระ
- Database transaction

**Flow:**
1. ลูกค้าเลือกสินค้าและจำนวนงวด (3, 6, 12)
2. คำนวณยอดผ่อนต่อเดือน = ยอดรวม ÷ จำนวนงวด
3. สร้าง order ใน database
4. สร้าง installment_payments สำหรับแต่ละงวด
5. ใช้ transaction เพื่อให้ข้อมูลสอดคล้องกัน

**Code ที่สำคัญ:**
```javascript
// คำนวณยอดผ่อน
const monthlyPayment = totalAmount / installmentPeriods;

// สร้าง installment payments
for (let i = 1; i <= installmentPeriods; i++) {
    const dueDate = new Date();
    dueDate.setMonth(dueDate.getMonth() + i);
    
    await connection.execute(
        'INSERT INTO installment_payments ...',
        [orderId, i, monthlyPayment, dueDate, 'pending']
    );
}
```

### 4. **ตะกร้าสินค้า (Cart System)**

**ตำแหน่ง:** `server.js` บรรทัด 1166-1290

**สิ่งที่ควรอธิบาย:**
- เก็บข้อมูลตะกร้าใน database (ไม่ใช่ localStorage)
- รองรับ pricing types หลายแบบ (cash, cashPromo, installment, installmentPromo)
- Unique constraint: (customer_id, product_id, pricing_type)

**Code ที่สำคัญ:**
```javascript
// เพิ่มสินค้าลงตะกร้า
INSERT INTO cart_item (customer_id, product_id, pricing_type, quantity, unit_price)
VALUES (?, ?, ?, ?, ?)
ON DUPLICATE KEY UPDATE quantity = quantity + ?

// ดึงสินค้าในตะกร้า
SELECT ci.*, p.product_name, p.product_image
FROM cart_item ci
JOIN product p ON ci.product_id = p.product_id
WHERE ci.customer_id = ?
```

### 5. **Role-Based Access Control**

**ตำแหน่ง:** `server.js` บรรทัด 1359-1367

**สิ่งที่ควรอธิบาย:**
- แยกสิทธิ์ระหว่าง Customer และ Admin
- Admin เท่านั้นที่สามารถ:
  - เพิ่ม/แก้ไข/ลบสินค้า
  - อนุมัติคำสั่งซื้อ
  - ดูลูกค้าทั้งหมด

**Code ที่สำคัญ:**
```javascript
// Middleware ตรวจสอบว่าเป็น Admin
const isAdmin = (req, res, next) => {
    if (req.user && req.user.role === 'admin') {
        next();
    } else {
        res.status(403).json({ error: 'Admin access required' });
    }
};

// ใช้กับ routes ที่ต้องการสิทธิ์ Admin
app.post('/api/products', authenticateToken, isAdmin, async (req, res) => {
    // ...
});
```

### 6. **SQL Injection Prevention**

**สิ่งที่ควรอธิบาย:**
- ใช้ parameterized queries แทน string concatenation
- ตัวอย่างที่ถูกต้อง vs ผิด

**Code ที่ถูกต้อง:**
```javascript
// ✅ ถูกต้อง - ใช้ parameterized query
await pool.execute(
    'SELECT * FROM customer WHERE customer_username = ?',
    [username]
);
```

**Code ที่ผิด:**
```javascript
// ❌ ผิด - เสี่ยง SQL Injection
await pool.execute(
    `SELECT * FROM customer WHERE customer_username = '${username}'`
);
```

### 7. **Error Handling**

**สิ่งที่ควรอธิบาย:**
- ทุก API endpoint มี try-catch
- ส่ง error message ที่เหมาะสม
- Log errors สำหรับ debugging

**Code Pattern:**
```javascript
app.post('/api/endpoint', async (req, res) => {
    try {
        // Business logic
        res.json({ success: true, data: result });
    } catch (error) {
        console.error('Error:', error);
        res.status(500).json({ error: 'Internal server error' });
    }
});
```

### 8. **Database Transactions**

**ตำแหน่ง:** `server.js` บรรทัด 2074-2194 (Create Order)

**สิ่งที่ควรอธิบาย:**
- Transaction คืออะไร
- ทำไมต้องใช้ transaction
- ตัวอย่าง: การสร้าง order ต้องสร้างทั้ง order, order_detail, และ installment_payments
- ถ้าเกิด error ต้อง rollback ทั้งหมด

**Code ที่สำคัญ:**
```javascript
const connection = await pool.getConnection();
await connection.beginTransaction();

try {
    // สร้าง order
    await connection.execute('INSERT INTO `order` ...');
    
    // สร้าง order_detail
    await connection.execute('INSERT INTO order_detail ...');
    
    // สร้าง installment_payments
    for (let i = 1; i <= periods; i++) {
        await connection.execute('INSERT INTO installment_payments ...');
    }
    
    await connection.commit();
} catch (error) {
    await connection.rollback();
    throw error;
} finally {
    connection.release();
}
```

---

## 🔐 Security Features

### 1. Password Security
- ✅ bcrypt hashing (salt rounds: 10)
- ✅ ไม่เก็บ plain text password
- ✅ รองรับ legacy plain text (auto-upgrade to bcrypt)

### 2. Authentication
- ✅ JWT tokens
- ✅ Token expiration (24 hours)
- ✅ Protected routes

### 3. Authorization
- ✅ Role-based access control
- ✅ Admin-only endpoints

### 4. Data Protection
- ✅ SQL injection prevention (parameterized queries)
- ✅ CORS configuration
- ✅ Environment variables สำหรับ sensitive data

### 5. Input Validation
- ✅ Email format validation
- ✅ Phone number sanitization
- ✅ File type validation (images only)
- ✅ File size limit (10MB)

---

## 🔄 Flow การทำงานของระบบ

### Flow: สมัครสมาชิก
1. User กรอกข้อมูลใน Register form
2. Frontend ส่ง POST `/api/customer/register`
3. Backend ตรวจสอบว่า username/email ซ้ำหรือไม่
4. Hash password ด้วย bcrypt
5. เก็บข้อมูลใน database
6. ส่ง response กลับไป Frontend
7. Frontend redirect ไปหน้า Login

### Flow: เข้าสู่ระบบ
1. User กรอก username/password
2. Frontend ส่ง POST `/api/customer/login`
3. Backend ตรวจสอบ username/password
4. สร้าง JWT token
5. ส่ง token และ user info กลับไป
6. Frontend เก็บ token ใน localStorage
7. Frontend redirect ไปหน้า Home

### Flow: สั่งซื้อสินค้า
1. User เพิ่มสินค้าลงตะกร้า
2. User ไปที่หน้า Cart
3. User เลือกวิธีการชำระเงินและจำนวนงวด
4. Frontend ส่ง POST `/api/orders`
5. Backend:
   - เริ่ม transaction
   - สร้าง order
   - สร้าง order_detail สำหรับแต่ละสินค้า
   - คำนวณยอดผ่อน
   - สร้าง installment_payments
   - ล้างตะกร้า
   - commit transaction
6. ส่ง response กลับไป
7. Frontend redirect ไปหน้า Orders

### Flow: อนุมัติคำสั่งซื้อ (Admin)
1. Admin ไปที่หน้า Admin Orders
2. Frontend ส่ง GET `/api/orders`
3. Backend ส่งรายการคำสั่งซื้อทั้งหมดกลับมา
4. Admin คลิก "อนุมัติ"
5. Frontend ส่ง PUT `/api/orders/approve/:id`
6. Backend:
   - ตรวจสอบว่าเป็น Admin
   - อัปเดต order_status เป็น 'approved'
   - ส่งอีเมลแจ้งลูกค้า (ถ้ามี SMTP config)
7. ส่ง response กลับไป
8. Frontend refresh รายการคำสั่งซื้อ

---

## 💡 จุดสำคัญที่อาจารย์อาจถาม

### 1. "ทำไมใช้ JWT แทน Session?"
**คำตอบ:**
- JWT เป็น stateless ไม่ต้องเก็บ session ใน server
- Scalable มากขึ้น (สามารถใช้หลาย server ได้)
- Frontend และ Backend แยกกันชัดเจน

### 2. "ทำไมต้อง hash password?"
**คำตอบ:**
- ป้องกันการ leak password ถ้า database ถูก hack
- bcrypt เป็น one-way hashing ไม่สามารถ reverse ได้
- มี salt เพื่อป้องกัน rainbow table attacks

### 3. "ทำไมต้องใช้ Transaction?"
**คำตอบ:**
- เพื่อให้ข้อมูลสอดคล้องกัน (data consistency)
- ถ้าเกิด error ระหว่างการสร้าง order ต้อง rollback ทั้งหมด
- ป้องกันข้อมูลไม่ครบถ้วน

### 4. "Connection Pool คืออะไร?"
**คำตอบ:**
- สร้าง pool ของ database connections ล่วงหน้า
- Reuse connections แทนการสร้างใหม่ทุกครั้ง
- เพิ่มประสิทธิภาพและลด overhead

### 5. "ทำไมเก็บตะกร้าใน Database แทน localStorage?"
**คำตอบ:**
- เก็บข้อมูลถาวร เปลี่ยน device ก็ยังมีข้อมูล
- หลายคนใช้ account เดียวกันได้
- ง่ายต่อการ backup และ restore

---

## 📝 Checklist สำหรับการพรีเซนต์

### ส่วนที่ต้องอธิบายได้:
- [ ] โครงสร้างโปรเจกต์
- [ ] Authentication (JWT)
- [ ] Password Hashing (bcrypt)
- [ ] ระบบผ่อนชำระ
- [ ] Database Schema
- [ ] API Endpoints
- [ ] Security Features
- [ ] Error Handling
- [ ] Database Transactions

### Code ที่ต้องอธิบายได้:
- [ ] Login/Register flow
- [ ] Cart system
- [ ] Order creation
- [ ] Installment calculation
- [ ] Admin approval
- [ ] Image upload

### ถ้าอาจารย์ให้แก้ไขโค้ด:
- [ ] รู้ว่าแต่ละส่วนอยู่ที่ไหน
- [ ] เข้าใจ flow การทำงาน
- [ ] สามารถเพิ่ม feature ใหม่ได้
- [ ] สามารถแก้ bug ได้

---

## 🎓 คำศัพท์ที่ควรรู้

- **JWT (JSON Web Token)**: Token สำหรับ authentication
- **bcrypt**: Algorithm สำหรับ hash password
- **Middleware**: Function ที่ทำงานระหว่าง request และ response
- **Connection Pool**: Pool ของ database connections
- **Transaction**: กลุ่มของ database operations ที่ต้องทำพร้อมกัน
- **RESTful API**: API design pattern ที่ใช้ HTTP methods
- **CORS**: Cross-Origin Resource Sharing
- **SQL Injection**: การโจมตีฐานข้อมูลโดยการ inject SQL code
- **Parameterized Query**: Query ที่ใช้ parameters แทน string concatenation

---

**เอกสารนี้พร้อมสำหรับการพรีเซนต์! 🎉**

